import { StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        justifyContent: 'center',
    },
    descriptionBold: {
        fontSize: 18,
        fontWeight: 'bold',
        color: '#808080',
    },
    descriptionNormal: {
        fontSize: 18,
        color: '#808080',
    }
});